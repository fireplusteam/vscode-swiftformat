{
  "name": "vscode-swiftformat-xcode",
  "displayName": "XCode Swift Formatter",
  "description": "Formatting Swift code with nicklockwood/SwiftFormat like in Xcode.",
  "repository": {
    "type": "git",
    "url": "https://github.com/fireplusteam/vscode-swiftformat.git"
  },
  "version": "1.7.7",
  "license": "MIT",
  "author": {
    "name": "Ievgenii Mykhalevskyi",
    "email": "fireplusteam@gmail.com",
    "url": "https://github.com/fireplusteam"
  },
  "publisher": "fireplusteam",
  "icon": "assets/icon.png",
  "galleryBanner": {
    "color": "#FC823F",
    "theme": "light"
  },
  "engines": {
    "vscode": "^1.86.0"
  },
  "categories": [
    "Formatters",
    "Programming Languages",
    "Other"
  ],
  "activationEvents": [
    "onLanguage:swift"
  ],
  "keywords": [
    "swift",
    "swiftformat",
    "prettifier",
    "formatter",
    "xcode"
  ],
  "main": "./out/src/extension",
  "contributes": {
    "languages": [
      {
        "id": "swift",
        "aliases": [
          "Swift"
        ],
        "extensions": [
          "swift"
        ]
      }
    ],
    "configuration": {
      "type": "object",
      "title": "SwiftFormat Configuration",
      "properties": {
        "xcode.swiftformat.enable": {
          "type": "boolean",
          "default": true,
          "description": "Whether SwiftFormat should actually start up on this project."
        },
        "xcode.swiftformat.onlyEnableOnSwiftPMProjects": {
          "type": "boolean",
          "default": false,
          "description": "Only allows the extension to load up when SwiftFormat is available via Swift PM."
        },
        "xcode.swiftformat.onlyEnableWithConfig": {
          "type": "boolean",
          "default": false,
          "description": "Only use SwiftFormat when a config exists."
        },
        "xcode.swiftformat.path": {
          "description": "The location of your globally installed SwiftFormat.",
          "scope": "machine-overridable",
          "default": [
            "/usr/bin/env",
            "swiftformat"
          ],
          "oneOf": [
            {
              "type": "string",
              "default": "/usr/local/bin/swiftformat"
            },
            {
              "type": "array",
              "minItems": 1,
              "default": [
                "/usr/bin/env",
                "swiftformat"
              ],
              "items": {
                "type": "string"
              }
            }
          ]
        },
        "xcode.swiftformat.options": {
          "type": "array",
          "default": [],
          "description": "Additional options for SwiftFormat: https://github.com/nicklockwood/SwiftFormat#options",
          "items": {
            "type": "string"
          }
        },
        "xcode.swiftformat.configSearchPaths": {
          "type": "array",
          "default": [
            ".swiftformat"
          ],
          "description": "Possible paths for SwiftFormat config.",
          "items": {
            "type": "string"
          }
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "format": "prettier --write ./*.json ./**/*.ts"
  },
  "devDependencies": {
    "@types/node": "^18.17.17",
    "@types/vscode": "^1.86.0",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "glob": "^10.3.4"
  }
}